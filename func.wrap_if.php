<?php

// --------------------------------------------------------------------------
// Обернуть строку, если она не пустая
// --------------------------------------------------------------------------

function wrap_if($t = '', $left = '', $right='') {
	$t = (string) $t;
	if($t) {
		return $left . $t . $right;
	}
	return '';
}

?>