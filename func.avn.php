<?php

// --------------------------------------------------------------------------
// Создание из хеша k=>array(v=>name) нового хеша name=>array(v=>name)
// --------------------------------------------------------------------------

function avn($arr, $name) {
	$r = array();
	foreach($arr as $k=>$v) {
		$r[$v[$name]] = $v;
	}
	return $r;
}

?>